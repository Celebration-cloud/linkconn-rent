/**
 * default.tsx — Parallel route fallback for the (auth) route group.
 *
 * Rendered when no active parallel route slot matches.
 * Reference: https://nextjs.org/docs/app/api-reference/file-conventions/default
 */
export default function AuthDefault() {
  return null;
}
