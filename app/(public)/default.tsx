/**
 * default.tsx — Parallel route fallback for the (public) route group.
 *
 * Next.js renders this when no matching slot is active for a parallel route.
 * Reference: https://nextjs.org/docs/app/api-reference/file-conventions/default
 */
export default function PublicDefault() {
  return null;
}
